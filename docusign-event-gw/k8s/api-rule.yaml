apiVersion: gateway.kyma-project.io/v1alpha1
kind: APIRule
metadata:
  name: docusign-event-gw
spec:
  gateway: kyma-gateway.kyma-system.svc.cluster.local
  rules:
    - accessStrategies:
        - config: {}
          handler: noop
      methods:
        - GET
        - POST
        - PUT
        - DELETE
      path: /.*
  service:
    host: docusign-event-gw
    name: docusign-event-gw
    port: 8080